{
  "name": "web",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "classnames": "^2.2.5",
    "mini.css": "^2.1.5",
    "react": "^15.5.4",
    "react-dom": "^15.5.4",
    "socket.io-client": "^1.7.3"
  },
  "devDependencies": {
    "react-scripts": "0.9.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && (rm -rf build-prod; mv build build-prod)",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "proxy": "http://localhost:5580"
}
